<%= render "/user/orders/show" %>

<% unless @order.shipped? || @order.cancelled? %>
  <center><%= button_to "Cancel Order", "/admin/users/#{@order.user_id}/orders/#{@order.id}", method: :patch%></center></br>
<% end %>