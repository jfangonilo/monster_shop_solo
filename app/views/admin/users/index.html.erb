<h1>All Users</h1>

<section class="grid-container">
  <% @users.each do |user| %>
    <section class="grid-item" id="user-<%= user.id %>">
      <p><%= link_to user.name, "/admin/users/#{user.id}" %></p>
      <p>Account Since: <%= user.created_at %></p>
      <p>Type: <%= user.role %></p>
    </section>
  <% end %>
</section>